<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Ttr.Ui</RootNamespace>
    <AssemblyName>ttr.Ui</AssemblyName>
    <!-- Required by the [LibraryImport] source generator for the Windows VT-enablement P/Invoke
         (WindowsTerminal.cs, CLAUDE.md invariant 4). Only permits the generated marshalling code. -->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../ttr.Core/ttr.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Spectre.Console is used ONLY as a measuring library (Segment.CellCount). -->
    <PackageReference Include="Spectre.Console" />
    <!-- ColorCode.Core: tokenisation for the 'o' modal's syntax highlighting (§11.5). -->
    <PackageReference Include="ColorCode.Core" />
  </ItemGroup>

</Project>
