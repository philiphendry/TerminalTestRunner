namespace TtrParser;

/// <summary>
/// Placeholder. The real file-reference parser (POC-9) is lifted verbatim once its
/// artifacts (library + corpus + tests) are available — see docs/ttr-implementation-plan.md §11.5.
/// </summary>
internal static class Placeholder
{
}
