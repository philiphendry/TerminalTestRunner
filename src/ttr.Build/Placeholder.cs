namespace Ttr.Build;

/// <summary>
/// Placeholder for the build subsystem (MSBuildLocator, evaluation, ProjectGraph,
/// build execution). Implemented from Phase 3 onward — see docs/ttr-implementation-plan.md §7.
/// Kept as a type so the assembly is non-empty and the project participates in the build.
/// </summary>
internal static class Placeholder
{
}
